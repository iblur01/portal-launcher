<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.PortalLauncher" parent="android:style/Theme.Material.NoActionBar">
        <!--
            A home activity must let Android's WallpaperService own the layer behind its window.
            Opaque in-app backgrounds still cover it; the "system" mode deliberately stays
            transparent so static, live and OEM wallpapers all render through the native path.
        -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">#64B5F6</item>
    </style>

    <!-- Pin-shortcut requests are accepted headlessly: no window should ever be seen. -->
    <style name="Theme.PortalLauncher.Invisible" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoDisplay">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- Transparent but displayable: unlike windowNoDisplay, this may stay alive during an API call. -->
    <style name="Theme.PortalLauncher.Provisioning" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>

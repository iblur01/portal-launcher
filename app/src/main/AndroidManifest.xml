<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Portal is usable on touch panels and Android TV / D-pad-only wall displays. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!--
        Deliberate kiosk capabilities. WRITE_SETTINGS is user-approved through the system screen;
        WRITE_SECURE_SETTINGS is optionally granted over ADB as documented in README.md.
    -->
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <queries>
        <package android:name="io.homeassistant.companion.android" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".PortalApp"
        android:allowBackup="false"
        android:banner="@drawable/tv_banner"
        android:usesCleartextTraffic="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PortalLauncher">

        <!--
            Home-activity attributes, mirroring what AOSP Launcher3 declares:
            stateNotNeeded  — the home activity can be started before its saved state is restorable
                              (early boot), and must not be killed for lacking it;
            taskAffinity="" — keeps launched apps out of the launcher's task;
            clearTaskOnLaunch — pressing HOME returns to the launcher root, not a stale sub-screen.
        -->
        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:clearTaskOnLaunch="true"
            android:taskAffinity=""
            android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets apps pin one of their shortcuts onto the grid; accepted without a window. -->
        <activity
            android:name=".PinShortcutActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.PortalLauncher.Invisible">
            <intent-filter>
                <action android:name="android.content.pm.action.CONFIRM_PIN_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!--
            First-run assistant. Not exported: it writes settings, so only Portal opens it. A
            debug-only activity-alias (app/src/debug/AndroidManifest.xml) exposes it to ADB for
            testing without shipping that entry point.
        -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <!-- ADB-only, headless Home Assistant provisioning for keyboard-less panels. -->
        <activity
            android:name=".ui.onboarding.AdbHomeAssistantProvisioningActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:permission="android.permission.DUMP"
            android:theme="@style/Theme.PortalLauncher.Provisioning">
            <intent-filter>
                <action android:name="com.iblu01.portallauncher.action.PROVISION_HOME_ASSISTANT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PlaygroundActivity"
            android:exported="false" />

        <activity
            android:name=".OpacityPreviewActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <activity
            android:name=".ClockThemeActivity"
            android:exported="false"
            android:screenOrientation="fullSensor" />

        <service
            android:name=".MqttBridgeService"
            android:exported="false" />

        <service
            android:name=".ScreenAccessibility"
            android:exported="true"
            android:label="@string/accessibility_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".SleepReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
